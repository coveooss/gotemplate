@merge($, $.common)

@template("draw_fir")
  @$colors := printf("Fg%s,Bg%s", .fg, .bg)
  @$len := .size * 2 * lenc(.char) + len(color($colors))
  @range($n := to(1, .size*2, 2))
    -@center(color($colors, $.char * $n), $len)@CR
  @end range
@end template

@func("ChristmasTree", "template", "draw_fir", "size=15 bg=Green fg=Red char='✾'", "[size, bg, fg, char]")
@func("Fir", "template", "draw_fir", "size=10 bg=Green fg=White char=' '", "[size, bg, fg, char]")
